import { BlogCategoryClient } from "./_components/blog-category-client";

export const metadata = {
  title: "Blog Categories | Admin Dashboard",
};

export default function BlogCategoryPage() {
  return <BlogCategoryClient />;
}
